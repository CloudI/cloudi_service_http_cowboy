language: erlang
otp_release:
  - 23.2.5
  - 22.3.4
  - 21.3.8.1
before_script:
  - rebar get-deps
script: rebar compile
after_script:
  - rebar clean
branches:
  only:
    - master
notifications:
  irc:
    channels:
      - "chat.freenode.net#cloudi"
    on_success: change
    on_failure: always
